template
  each widget, index in widgets
    #{`gwi-${widget.name}`}(:data=`widgets[${index}].data`)
=`\n`
script
  = `\n`
  = `  import widgets from 'data.json'`
  each widget, index in widgets
    = `\n`
    = `  import ${widget.name} from 'widgets/${widget.name}/${widget.name}.vue'`
  = `\n\n`
  = `  export default {\n`
  = `    components: {\n`
  each widget, index in widgets
    = `      'gwi-${widget.name}': ${widget.name}${index !== (widget.length - 1) ? ',' : ''}\n`
  = `    },\n`
  = `    data: {\n`
  = `      widgets\n`
  = `    }\n`
  = `  }`
=`\n`
style
    |@import 'index.less';